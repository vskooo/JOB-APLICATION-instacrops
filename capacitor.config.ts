import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'instacrops.favio',
  appName: 'instacrops',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
